#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _abs - writes the character c to stdout
 * @n: The character to print
 *
 * Return: On success.
 */

int _abs(int n)
{
	return (abs(n));
}
